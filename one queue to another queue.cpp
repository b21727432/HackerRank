#include <queue>


int main(){
	
	std::queue<int> kareliqueue;
	while (!verilenqueue.empty())
  {
  	kareliqueue.push(verilenqueue.front() ** 2);
  	std::cout << ' ' << verilenqueue.front();
    verilenqueue.pop();
  }
	
	while (!kareliqueue.empty())
  {
  	std::cout << ' ' << kareliqueue.front();
    kareliqueue.pop();
  }
	return 0;
}
